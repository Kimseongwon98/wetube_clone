extends layouts/main.pug
include mixin/videoBlock.pug
block content 
    .videos
        each video in dbVideos
            +videoBlock({
                id: video.id,
                title:video.title,
                views:video.views,
                videoFile:video.videoFile
            })
